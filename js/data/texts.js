// MUSEUM MESSAGES

const INTRO_MESSAGE = [`Here you finally are at the Museum of Modern Art in New York, ready to experience the performance work "The Artist Is Present" by artist Marina Abramovic.`, `(Press ARROW KEYS to move, press again to stop. Press ENTER to skip through these messages)`];

const LEAVING_MOMA_ON_FOOT_MESSAGE = [`You remember you came here to see Marina Abramovic and decide not to leave just yet.`];

const MOMA_CLOSED_MESSAGE = [
  `The Museum of Modern Art is closed.

Our hours are:

Wednesday - Monday
10:30AM to 5:30PM.

Closed Christmas and Thanksgiving.`
];

const JOIN_TICKET_QUEUE_MESSAGE = [`You join the queue to purchase a ticket to see Marina Abramovic.`];
const BUY_TICKET_MESSAGE = [`That will be $25.00, please...`, `Thank you.`];
const ALREADY_BOUGHT_TICKET_MESSAGE = [`Excuse me, you have a ticket! Just go in.`];
const WRONG_TICKET_QUEUE_ENTRY_MESSAGE = [`Excuse me, the line starts to the left!`];
const LEAVING_TICKET_SCREEN = [`As you start to wander off, you realise that you can check out the rest of the museum some other time...`];

const TICKETS_GUARD_WELCOME_MESSAGE = [`Ticket please...`, `Thank you.`];
const TICKETS_GUARD_NO_TICKET_MESSAGE = [`You need a ticket to enter.`, `The desk is right over there.`];

// THE ARTWORKS
const STARRY_NIGHT = ["Starry Night\nVincent Van Gogh\n1889\nOil on canvas"];
const OLIVE_TREES = ["The Olive Trees with the Alpilles in the Background\nVincent Van Gogh\n1889\nOil on canvas"];
const SOUP_CANS = ["Campbell's Soup Cans\nAndy Warhol\n1962\nSynthetic polymer paint on canvas"];
const DANCERS = ["The Dance I\nHenry Matisse\n1909\nOil on canvas"];

// THE PERFORMANCE AREA
const TAPE_MESSAGE = [`Seems like it would be rude to just walk across the tape...`];
const LEAVE_MARINA_AREA = [
  `Seems like it would be pointless to just wander off now...`
];

// THE PERFORMANCE QUEUE
const JOIN_MARINA_QUEUE_MESSAGE = [`You join the queue to see Marina Abramovic. Now all you have to do is wait.`];
const LEAVE_MARINA_QUEUE_MESSAGE = [`You leave the queue. Maybe another time.`];
// This is what the atrium guards say
const GUARD_INSTRUCTIONS = [
  `"Hi. You're next."`,
  `"There are just a few simple rules I need to give you."`,
  `"Sit still, don't move around."`,
  `"Don't talk to her."`,
  `"Maintain eye contact the entire time you are seated."`,
  `"That's it. You can go."`
];
const PLEASE_WAIT = [
  `"Please wait here until it's your turn."`
];
const SIT_FAIL = [
  `Having watched you fool around for far too long without taking your seat, a security guard escorts you off the premises.`
];

// CLOSING TIME
const MUSEUM_CLOSING = [
  `"Attention all patrons, The Museum of Modern Art will be closing in fifteen minutes."`
]
const MUSEUM_CLOSED = [
  `The Museum of Modern Art is now closed. Have a nice day.`
];

// BAD BEHAVIOUR
const EXCUSE_ME_MESSAGE = [
  `"Excuse me..."`
];
const SHOVING_MESSAGE = [
  `Having been shoved by you far too many times, the museum patron alerts a security guard who politely escorts you off the premises.`
];
const SLOW_QUEUEING_MESSAGE = [
  `"If you can't queue properly then get out of the queue!"`
];
const OBSTRUCTION_MESSAGE = [
  `Museum security observes your total unwillingness to move aside for other patrons and a security guard politely escorts you off the premises.`
];

// DEBUG
const MARINA_Q_SYMBOL = `MQ`;
const TICKET_Q_SYMBOL = `TQ`;
const NO_Q_SYMBOL = `!Q`;